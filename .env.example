# Copy to .env and fill in. `.env` is gitignored.

# ─── Dashboard accounts ──────────────────────────────────
# On first start with no accounts, this creates the first Owner (username
# `admin` unless PANEL_ADMIN_USER says otherwise). After that it is ignored —
# manage people from the Users tab. Must meet MIN_PASSWORD_LENGTH.
PANEL_PASSWORD=change-me-to-something-long
# PANEL_ADMIN_USER=admin

# Sessions are server-side and revocable; there is no signing key to configure.
# SESSION_TTL_HOURS=12
# MIN_PASSWORD_LENGTH=10

# How long the audit log keeps entries.
# AUDIT_RETENTION_DAYS=180

# Backup retention. Backups thin out rather than truncate.
# BACKUP_KEEP_LATEST=5
# BACKUP_KEEP_DAILY=7
# BACKUP_KEEP_WEEKLY=4
# BACKUP_MAX_TOTAL=50
# Rollback points taken before an edit are never pruned this fresh.
# BACKUP_SAFETY_GRACE_HOURS=48

# ─── The write ceiling ───────────────────────────────────
# `readonly` | `safe` | `full`. **Defaults to `safe` when unset**, which is why
# the dashboard reports a security level you never configured.
#
# This is a ceiling the web UI cannot raise, so it is a decision made on the host
# rather than in a browser. At `safe`, save editing is unavailable **even to an
# Owner** — the editor is hidden and the locked card names this variable. Raise it
# to `full` deliberately, when you actually want to edit saves.
#
# Anything unrecognised falls back to `safe` with a warning in the log.
# SECURITY_LEVEL=safe

# Read-only guest view: map, server status, base locations.
GUEST_VIEW_ENABLED=true

# Who sees map content nobody has discovered yet — fast-travel points and
# effigies a player has not found. `everyone`, `nobody`, or a role name meaning
# that rank and above. Whether undiscovered content is a spoiler is a question
# about how you run your server, not a security one, hence a setting.
# Filtering happens server-side: a session that may not see them is never sent
# them, so the answers are not sitting in the network tab.
# DISCOVERY_VISIBILITY=detail

# Ceiling on any upload the backend accepts (world exports, import documents).
# An unbounded read into memory is a denial of service against the machine
# running your game server, which is the thing this dashboard exists not to
# disturb.
# MAX_UPLOAD_MB=64

# Leave unset to infer from the request (X-Forwarded-Proto or the URL scheme),
# which is right for both plain-http LAN and a TLS reverse proxy. Force it only
# if your proxy does not set the header.
# COOKIE_SECURE=true

# ─── Palworld server ─────────────────────────────────────
# Must match AdminPassword in PalWorldSettings.ini, and the server needs
# RESTAPIEnabled=True.
PALWORLD_ADMIN_PASSWORD=your-admin-password

# With the bundled compose file this is the service name on the private
# network. Do not publish port 8212 to the internet.
PALWORLD_REST_URL=http://palworld:8212

# ─── Who owns PalWorldSettings.ini ───────────────────────
# `true` stops thijsvanloef regenerating the INI from these variables on every
# start, which is what makes the dashboard's Settings tab stick. Requires image
# 0.24.0 or newer.
#
# With it true the game-setting env vars are ignored and the INI rules; with it
# false the INI is rewritten on every restart and anything the dashboard wrote is
# discarded. The dashboard cannot see which way this is set.
DISABLE_GENERATE_SETTINGS=true

SERVER_NAME=My Palworld Server

# ─── Paths inside the container ──────────────────────────
SAVE_BASE_DIR=/palworld/Pal/Saved/SaveGames/0
# Kept off the server directory so mounting /palworld read-only still allows
# backups.
BACKUP_DIR=/app/backups
CACHE_DIR=/app/cache
# Auto-detected from SAVE_BASE_DIR; set only if your layout is unusual.
# PALWORLD_CONFIG_INI=/palworld/Pal/Saved/Config/LinuxServer/PalWorldSettings.ini
# Pin a world if you have more than one; otherwise the most recently written wins.
# WORLD_GUID=

# ─── Corruption safety ───────────────────────────────────
# Hard lock: never write to save files, even when the server is provably down.
SAVE_READ_ONLY=false

# A .sav written within this many seconds means the server is alive.
SAVE_ACTIVITY_WINDOW_SECONDS=300

# Leave false. True lets an INCONCLUSIVE server-state check count as "stopped",
# which is exactly how a live world gets corrupted.
ALLOW_UNVERIFIED_EDITS=false

# ─── Performance ─────────────────────────────────────────
# Level.sav parsing. Turn off and the live REST features still work fully.
PARSE_ENABLED=true

# false (default) = nothing parses on its own; the save is read ONLY when you
# press Refresh. Reads serve cached data and never trigger work. Set true to
# also let stale data refresh itself in the background.
PARSE_AUTO=false

# Minimum seconds between automatic parses. Only applies when PARSE_AUTO=true;
# manual Refresh always parses immediately.
PARSE_MIN_INTERVAL_SECONDS=900

# Give up on a parse after this long.
PARSE_TIMEOUT_SECONDS=600

# Decode chest/inventory contents. Measured at ~2% extra cost, so leave on
# unless you are very tight on memory.
PARSE_INCLUDE_ITEMS=true

# Refuse to parse a Level.sav larger than this (MB). Peak memory runs roughly
# 8x the *decompressed* size.
PARSE_MAX_SIZE_MB=1024

# Defer a parse when the game server is already struggling. Gameplay wins over
# dashboard responsiveness.
#
# This gates the START of a parse and never interrupts one in flight — a running
# parse has already paid most of its cost and is niced, so killing it wastes the
# work and frees capacity only until the next request begins again.
#
# It fails OPEN, unlike the save-write guard: no metrics, a stale sample or an
# unreachable server all read as "fine to parse". Only a live reading below the
# floor defers anything.
PARSE_LOAD_AWARE=true
# Automatic parses stop below this server FPS. Palworld targets 30; players report
# lag in the sustained-under-20 range.
PARSE_MIN_SERVER_FPS=20
# A manual Refresh gets more latitude — the operator asked and is watching, so
# overriding them needs the server to be in real trouble rather than merely busy.
PARSE_FORCE_MIN_SERVER_FPS=12

# ─── Metrics history (Phase 8) ───────────────────────────
# Samples server FPS, players, CPU, memory, disk and world size into SQLite.
# Rows are kept raw with no rollup: at 60s that is ~43,000 rows for 30 days,
# which SQLite answers instantly.
#
# A sample is written even when the game is unreachable, so an outage draws as a
# gap rather than being interpolated over.
METRICS_ENABLED=true
METRICS_INTERVAL_SECONDS=60
METRICS_RETENTION_DAYS=30

# How long to wait on the game's REST API for a command (kick/ban/announce/save).
GAME_API_TIMEOUT_SECONDS=10

# ─── Game-update detection ───────────────────────────────
# The dashboard bundles positions read out of the game's own files: 396 effigies,
# 35,687 ore nodes / chests / fishing spots / oil fields. Those are static per game
# build, so a content patch can move them and nothing in a save file says so.
#
# The check is a *version tag* read: `buildid` out of the server's own
# steamapps/appmanifest_2394010.acf, plus the main pak's size and mtime. ~0.05 ms,
# no network. Only when that changes does anything expensive become necessary —
# re-extracting the positions walks 9,977 cell packages and takes minutes, so it
# is never done automatically. A detected change raises a banner pointing at
# `python3 scripts/check-game-build.py --extract`, which diffs old against new.
#
# Normally auto-detected from SAVE_BASE_DIR (the install root is four levels up).
# Set this if your layout differs, or if only the save subdirectory is mounted —
# without it the dashboard reports "cannot verify" rather than guessing.
# PALWORLD_INSTALL_DIR=/palworld
#
# How often to re-read the version tag. Default 6h. The first check after startup
# always runs regardless, which is the one that matters: an auto-updating server
# container updates and restarts, so boot is when a new build is there to find.
# GAME_BUILD_CHECK_INTERVAL_SECONDS=21600

# Where the game keeps its ban list. Auto-detected beside PalWorldSettings.ini;
# set this only if yours lives elsewhere. Read-only — the dashboard never writes
# it, and never mirrors it into its own database.
# PALWORLD_BANLIST=/palworld/Pal/Saved/Config/LinuxServer/banlist.txt

# ─── Restarting the game server (optional) ───────────────
# The REST API can stop the server but never start it. Leave unset if your
# server container exits when PalServer exits (its restart policy handles it).
# Set it if your container keeps running with the game process dead.
# Prefer a docker-socket-proxy over mounting the raw socket — see README.
#
# NOTE the `docker` CLI is deliberately NOT installed in the runtime image, so a
# command starting with `docker` fails with "not found". Use `node`, which is in
# the image and has a global fetch; docs/DEPLOYMENT.md §4 has the exact commands
# for stop/start/restart and explains why 304 counts as success.
#
# Also worth knowing before enabling any of this: ANY access to a Docker socket is
# host-root-capable, including through a proxy. Announce-and-shutdown already works
# with no Docker at all, and save editing unlocks by itself once the server is
# provably down — so the only thing this buys is a "start it again" button.
# RESTART_COMMAND=node -e "fetch('http://docker-proxy:2375/containers/palworld-server/restart',{method:'POST'}).then(r=>process.exit(r.status<300||r.status===304?0:1)).catch(()=>process.exit(1))"
RESTART_COMMAND=

# Stops the whole container, not just the game process. This is the clean way to
# prepare for save edits — a stopped container cannot relaunch the server
# underneath an in-progress write.
#
# NOTE the `node -e` form. The `docker` CLI is deliberately NOT installed in the
# runtime image (~35 MB for one optional feature), so a `docker ...` command here
# fails with "STOP_COMMAND not found: docker". It is not needed: the socket proxy
# speaks the Docker HTTP API and the image is node:20, which has a global fetch.
# `304` counts as success because the API returns it when the container was
# already in the requested state — treating that as a failure would error out
# exactly when "stop the server" has already been achieved.
# STOP_COMMAND=node -e "fetch('http://docker-proxy:2375/containers/palworld-server/stop',{method:'POST'}).then(r=>process.exit(r.status<300||r.status===304?0:1)).catch(()=>process.exit(1))"
STOP_COMMAND=

# Brings it back after maintenance, completing the stop -> edit -> start cycle
# from the dashboard. Without it, run `docker compose start palworld` yourself.
# START_COMMAND=node -e "fetch('http://docker-proxy:2375/containers/palworld-server/start',{method:'POST'}).then(r=>process.exit(r.status<300||r.status===304?0:1)).catch(()=>process.exit(1))"
START_COMMAND=

# How long to watch for the server to come back after a shutdown.
SERVER_RETURN_WATCH_SECONDS=180

# ─── Internal ────────────────────────────────────────────
PYTHON_BACKEND_URL=http://127.0.0.1:8400
BACKEND_PORT=8400
BACKEND_HOST=127.0.0.1

# Where world copies are written (the uid-remap export). Defaults to
# $BACKUP_DIR/exports. These are full worlds — a few MB each — and they are never
# pruned automatically, because a copy someone is about to carry off the server is
# not something a retention policy should decide to delete.
# SOLO_EXPORT_DIR=/app/exports

# SQLite file holding accounts, sessions, the audit log, metrics and schedules.
# Defaults to $CACHE_DIR/dashboard.db — keep it on the same volume, or a restart
# loses your logins and audit history.
# DASHBOARD_DB=/app/cache/dashboard.db

# Where the security policy is persisted. Defaults to $CACHE_DIR/policy.json.
# POLICY_FILE=/app/cache/policy.json

# ─── Rarely changed ──────────────────────────────────────
# Defaults are right for a normal 1.0 server. Listed so that finding them does
# not require reading the source.

# How long a single safety probe may take before it counts as "no answer" —
# which resolves to "running", because this guard fails closed.
# SAFETY_PROBE_TIMEOUT=5

# Re-reads when a .sav looks torn (the game was mid-write). Not a retry of a
# failure: it is how a partially-written file is distinguished from a corrupt one.
# SAVE_READ_RETRIES=3

# The level cap the editor validates against. 1.0 raised it from 65 to 80 —
# NOT the 100 rows in the EXP table, which carries headroom past the cap.
# PALWORLD_MAX_LEVEL=80

# Breeding path search bounds. A full unbounded walk over 46,655 pairs would
# happily eat the CPU the game server needs.
# BREEDING_MAX_DEPTH=4
# BREEDING_MAX_FRONTIER=120

# Per-category minimum role for static world objects (ore, chests, fishing
# spots), as JSON. Set from the Access tab normally; the environment is a
# ceiling the UI cannot raise. On a server where finding things is the point,
# handing every Player a complete ore map is a decision, not a default.
# WORLD_OBJECT_VISIBILITY={"ore":"trusted","treasure":"trusted"}

# How long a configured RESTART_COMMAND may run before it is abandoned.
# RESTART_COMMAND_TIMEOUT=120
