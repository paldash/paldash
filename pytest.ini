[pytest]
testpaths = backend/tests
pythonpath = backend
addopts = -q --strict-markers
markers =
    integration: needs refworld/ (a real save); skipped when absent
    slow: parses or writes a full world; several seconds each
filterwarnings =
    ignore::DeprecationWarning
